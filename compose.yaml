services:
  rio:
    build: .
    restart: unless-stopped
    env_file: .env.local
    environment:
      DATABASE_PATH: /app/data/rio.sqlite3
    volumes:
      - rio-data:/app/data
    stop_grace_period: 60s
volumes:
  rio-data:
